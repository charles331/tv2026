{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "conventionalcommits",
        "releaseRules": [
          { "breaking": true, "release": "major" },
          { "type": "feat", "release": "minor" },
          { "type": "fix", "release": "patch" },
          { "type": "perf", "release": "patch" },
          { "revert": true, "release": "patch" }
        ]
      }
    ],
    [
      "@semantic-release/release-notes-generator",
      { "preset": "conventionalcommits" }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "npm version ${nextRelease.version} --no-git-tag-version --allow-same-version && pnpm run build && pnpm exec electron-builder --win --config electron-builder.yml --publish never"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          { "path": "release/*-x64.exe", "label": "Installeur Windows x64 (.exe)" },
          { "path": "release/*-portable.exe", "label": "Version portable Windows x64 (.exe)" },
          { "path": "release/*.exe.blockmap" },
          { "path": "release/latest.yml" }
        ],
        "successComment": false,
        "failComment": false
      }
    ]
  ]
}
